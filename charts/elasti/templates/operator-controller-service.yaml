apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasti.fullname" . }}-operator-controller-service
  labels:
    control-plane: controller-manager
  {{- include "elasti.labels" . | nindent 4 }}
spec:
  type: {{ .Values.operatorControllerService.type }}
  selector:
    control-plane: controller-manager
  {{- include "elasti.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.operatorControllerService.ports | toYaml | nindent 2 }}